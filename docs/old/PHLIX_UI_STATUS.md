# Phlix UI Coverage — Build Status

> Single source of truth for progress. Every subagent reads its row first and writes its result back.
> On restart, resume at the first step whose state is not `done`.
> States: `todo | implementing | review | fixing | testing | documenting | merging | done | blocked | verifying`.
> Phase Coordinators add rows (and split items into `Na/Nb/…`) as they reach each phase.

| step-id | title | repo | state | PR | coverage% | notes |
|---|---|---|---|---|---|---|
| 0.1 | Upgrade Webman 2.2 / Workerman 5.1 | server+hub | done | https://github.com/detain/phlix-server/pull/125 + https://github.com/detain/phlix-hub/pull/29 | n/a (version bump) | verified 2026-05-27: server phpunit/stan-L9/psr12 green (2517 tests, 0 fail); hub phpunit/stan/cs green (558 tests, 0 fail). Carry-overs (NOT 0.1 blockers): psalm needs PHP ≥8.3.16; 2 PSR-4 namespace mismatches in tests; 8 line-length warnings in src/Dlna/* |
| 0.2 | Enable coroutine runtime (Swoole eventLoop + Context) | server+hub | done | (see 0.2a/b/c + server cumulative fix #128) | (see 0.2a/b/c) | all sub-steps merged 2026-05-27; both repos on clean+green master |
| 0.2a | Server entry-point Swoole hook (start.php + index.php) | server | done | https://github.com/detain/phlix-server/pull/126 | 0% (no tests in this PR) | shipped with a bug — `Worker::$eventLoop` (instance) instead of `Worker::$eventLoopClass` (static) — fixed in PR #128 (server cumulative pass) |
| 0.2b | Server Context audit + tests + bench + dev docs | server | done | https://github.com/detain/phlix-server/pull/127 + https://github.com/detain/phlix-docs/pull/18 | 100% on new code (RequestContext 7/7 stmts + 5/5 methods; AdminMiddleware 22/22 stmts + 3/3 methods) | merged 2026-05-27; src/Server/ audit found 0 offenders → shipped demonstrative RequestContext wrapper + AdminMiddleware publish + 10 isolation tests + coroutine_bench.php (≈3.9× speedup vs serialized); docs page at phlix-docs/docs/dev/coroutine-runtime.md |
| 0.2c-server-cumulative | Server cumulative fix: `Worker::$eventLoopClass` (was `$eventLoop`) + regression test | server | done | https://github.com/detain/phlix-server/pull/128 | n/a (1-line fix + 86-line guard test) | merged 2026-05-27; caught by cumulative review after hub 0.2c shipped the correct identifier from the start |
| 0.2c | Hub coroutine bootstrap + Context audit + tests + docs | hub | done | https://github.com/detain/phlix-hub/pull/30 | 100% on RequestContext (10 isolation tests + 2 AuthMiddleware tests); hub suite 570 tests / 0 fail / +12 from 0.2c | merged 2026-05-27; mirrors server 0.2b: Worker::$eventLoopClass + SWOOLE_HOOK_ALL with graceful fallback in start.php; src/Http/RequestContext.php with setUserId/getUserId/hasUserId/clearUserId; AuthMiddleware publishes user-id on successful auth, no-op on reject paths; required a follow-up commit (psalm @MixedAssignment suppression — hub psalm.xml at errorLevel=1, strictest). Docs reconciliation: hub RequestContext wrapper documentation (orphaned in live phlix-docs working tree) landed 2026-05-27 via https://github.com/detain/phlix-docs/pull/20 |
| 0.3 | Swoole + uv in install.sh / systemd / CI + preflight | server+hub | done | https://github.com/detain/phlix-server/pull/129 + https://github.com/detain/phlix-hub/pull/31 + https://github.com/detain/phlix-docs/pull/19 | n/a (shell+CI; no PHP code) | merged 2026-05-27 (server 0936dd1, hub a7ea443, docs e143b28). Idempotent swoole+uv source-build (verbatim Dockerfile.base flags) in install.sh ×2 + systemd.sh + disable_functions preflight. CI de-containerized (base-image approach reverted to avoid 127.0.0.1 integration self-skip): PHPUnit jobs load swoole via setup-php + uv via source-build → PHPUnit GREEN in real CI (server 2m33s, hub 42s). Hub+docs committed via throwaway clones (caliber pre-commit hook corrupted the live hub index — healed via rm .git/index+read-tree HEAD). Deferred: Finding 2 (hub CI has no mysql svc → hub DB-integration tests self-skip, pre-existing). Cumulative 0.1+0.2+0.3 pass NOT run (stopped per detain; 0.3 isolated infra). |
| 0.4 | Admin SPA scaffold (Vite+React+TS) + shell route + auth | server | done | https://github.com/detain/phlix-server/pull/130 + https://github.com/detain/phlix-docs/pull/21 | SPA 99.06% stmts/lines, 95.11% branch (55 Vitest tests); AdminAppController PHP 5/5 | merged 2026-05-27 (server f6ce496, docs #21). Unblocked by Master Coordinator running the toolchain the subagent was denied (npm/vendor-bin/gh work at top level). Independent code-reviewer caught 2 BLOCKING bugs the green Vitest suite HID (mocks encoded a fabricated auth/me contract): (1) GET /api/v1/auth/me returns `{user:{...}}` (not flat) → getCurrentUser now unwraps; (2) is_admin is DB TINYINT 1/0 (not JSON true) → added normalizeBool(), normalize at the API boundary + isAdminUser. Also fixed 3 verification-gap bugs npm exposed: tsc strict undefined in test helper, index.html entry-script base path, snake_case→camelCase PHP test names. CI green: PHPUnit/PHPStan-L9/PHPCS/Psalm/Component Tests/Security Audit/Composer all pass (Docker push + Codacy = known non-blocking noise). Built bundle committed to public/assets/admin/ (node_modules gitignored). CARRY-OVERS: (a) NO admin-ui CI job — SPA build+Vitest only verified locally, not in CI (add early in Phase 1 before SPA grows); (b) prod bundle ships a 741KB sourcemap (vite sourcemap:true) → git churn per build, consider sourcemap:false; (c) phlix-docs coroutine-runtime.md (0.2b) is not in the VitePress sidebar (pre-existing nav gap, flagged by Scribe). |
| 0.5 | Server-wide settings store (+ /api/v1/admin/settings) | server | done | https://github.com/detain/phlix-server/pull/132 + https://github.com/detain/phlix-docs/pull/22 | SettingsRepository 100% stmts, AdminSettingsController 98.8% (1 defensively-unreachable json arm) | merged 2026-05-27 (server 6e545ea, docs bcc9554). migration 026_server_settings.sql (typed key/value, CREATE TABLE IF NOT EXISTS); SettingsRepository (config default <- DB override = effective; dotted keys -> config/<file>.php; Workerman MySQL upsert; traversal-jailed); AdminSettingsController GET/PUT /api/v1/admin/settings (AdminMiddleware-gated 401/403, typed allow-list validation, unknown-key/wrong-type -> 400, all-or-nothing); routes in AdminRoutes group; DI bindings. API ONLY — settings UI is Phase 1.3; inline allow-list to be replaced/backed by 0.7 shared schema. CI full PHPUnit (2570 tests, real DB, migration applied) + PHPStan-L9 + PHPCS + Security Audit + Component Tests GREEN. Independent reviewer found 0 blocking (traced real Connection/route-gating/migration/config-key contracts). Fixer (top-level) resolved: 6 PHPStan-L9 mixed-cast errors (root-cause narrowing, no suppression), dropped `final` (sibling mockable-repo convention), integration-mock signature (real query() has $params=null default), and a CI-caught regression — AdminRoutesTest's hand-rolled container needed an AdminSettingsController binding (register() resolves all admin controllers at bind time). NOTE: phlix-server Psalm CI job is a NO-OP (psalm not installed; guarded `if [ -f vendor/bin/psalm ]`). |
| 0.6 | Filesystem-browse endpoint (/api/v1/admin/fs/browse) | server | done | https://github.com/detain/phlix-server/pull/133 + https://github.com/detain/phlix-docs/pull/23 | FsBrowseController 91.1% stmts (72/79); 7 uncovered = defensively-unreachable catch(Throwable)→500 + scandir()===false | merged 2026-05-27 (server 3e29b0f, docs 1ac9b6c). GET /api/v1/admin/fs/browse?path= lists immediate SUBDIRS only (no files/read/write/delete), AdminMiddleware-gated (401/403). Traversal jail mirrors AudiobookController::validateMediaPath: realpath() then `$real===$root \|\| str_starts_with($real.'/', $root.'/')` (NEVER str_contains) → ../ escape + symlink escape + non-allowed root all 403; non-existent 404; not-a-dir 400; empty path 200 roots list. Roots from new config/filesystem.php (browse_roots default /home /mnt /media /data; no env override — explicit boundary). Bound via factory() in AdminServicesProvider (non-resolving roots dropped at ctor). Independent reviewer NO BLOCKING (traced real Request/Response/route-gating/DI contracts, NOT mocks — the 0.4 fabricated-contract class absent here). AdminRoutesTest hand-rolled container stubbed for the new controller (AdminRoutes::register eagerly resolves all admin controllers — the 0.5 CI trap, pre-empted). CI full PHPUnit (2552 unit local + real-DB suite in CI) + PHPStan-L9 + PHPCS + Psalm(no-op) + Security Audit + Component Tests GREEN; Docker push + Codacy = known non-blocking noise. API ONLY — path-picker/library UI is Phase 1.1. |
| 0.7 | Shared schemas/DTOs (server-settings + webhook events) | shared+server+docs | done | shared https://github.com/detain/phlix-shared/pull/7 (merged, tagged v0.7.0) + server https://github.com/detain/phlix-server/pull/135 + docs https://github.com/detain/phlix-docs/pull/24 (+ prereq security https://github.com/detain/phlix-server/pull/134) | SchemaPaths 100%; AdminSettingsController 106/112 (defensive fail-safe arms + 1 pre-existing 0.5 arm uncovered); phlix-shared 276 green, phlix-server full Unit 2555 green | DONE 2026-05-27. PR-A (shared): server-settings.schema.json (15 keys, single source of truth) + webhook-events.json (7 supported webhook types + reserved webhook.test; DISTINCT from EventNameMap's 12 phlix.* plugin aliases) + SchemaPaths helper + Version 0.7.0; Psalm errorLevel-1 + PHPStan L9 green. PR-B (server): composer ^0.7.0 + AdminSettingsController derives allowedKeys() from the vendored schema (// 0.7: seam removed); GET/PUT behavior unchanged (15 behavior tests + lock-in green); full real-DB CI green. PR-C (docs): dev/shared-schemas.md + reconciled admin/server-settings.md. Decisions (detain): webhook catalog only (no WS-event catalog); catalog the 7 SUPPORTED types. CARRY-OVERS: (1.4) webhook event EMISSION unwired — only webhook.test fires today, wire real emission + events[] validation in Phase 1.4; (1.3) SPA settings form renders from the schema (carries range constraints PUT doesn't yet enforce). Prereq: CVE-2026-48736 symfony/http-foundation 7.4.8→7.4.13 shipped separately (#134). Hub has NO webhook surface (untouched). |
| 0.8 | webman/console CLI baseline (replaces scripts/*.php) | server+hub+docs | done | 0.8a server #136 + 0.8b server #137 + 0.8c hub #32 + 0.8d docs #25 (all merged) | server cmds 100% (12 classes), MigrationRunner 98%; hub MigrateCommand 100%, SmokeJwtCommand 80% | DONE 2026-05-27. server bin/phlix=12 cmds (migrate/library:*/plugin:*/backup:*/hwaccel:probe/user:reset-password); hub bin/phlix=migrate+smoke:jwt; both `list` DB-free. masters: server 397808d, hub 11413a8, docs ad89d63. Deferred (kept as scripts): hub:claim/claim-subdomain (net+TLS), port-forward (UPnP), marker-worker (broken+daemon). OUT-OF-BAND during 0.8: security+bug fix server #138→7e958fe (theme-media scan/delete now admin-gated [was UNAUTH]; Range header case-insensitive fix). Codex-flagged follow-ups STILL OPEN: hardcoded 127.0.0.1 DB creds in media getters (P1), TMDB cfg not merged at bootstrap (P2), HLS route/segment naming (P1), hub relay_url domain mismatch (P1); open dependabot Vite 5→8 server #131. |
| 0.9 | View layer → Twig (decision; else keep Smarty) | server | deferred | n/a (no code; decision gate) | n/a | DEFERRED 2026-05-27 (detain-confirmed) — keep Smarty via PageRenderer. Scoping: 47 .tpl/~2962 lines + 12 src files instantiate \Smarty (no config/view.php) → multi-PR parity rewrite, >>1-PR, regression risk across whole viewer portal. Benefit marginal: speed negligible (plan §view-decision); the XSS/auto-escape win is already covered by an audited explicit per-value `|escape:'html'`/`|escape:'url'` convention (Smarty `escape_html` defaults false & is NEVER set true in src). Spec/§7 explicitly sanction deferral; do NOT block Phase 1. Carry-overs for a future un-deferral (see steps/0.9-view-layer-twig.worklog.md): (a) STALE docblock in PageRenderer::renderTemplate() claims "default-on escape_html" — false, fix it; (b) theme_css/theme_js are a `{literal}{$..|raw}{/literal}` verbatim-token + ThemeMiddleware str_replace post-render injection hack (src/Theming/ThemeMiddleware.php:87-102) that any Twig port must preserve or redesign; (c) custom Smarty modifiers ({format} etc.) need Twig equivalents. |
| 1.0 | admin-ui CI gate (build + Vitest) | server | done | https://github.com/detain/phlix-server/pull/139 | n/a (CI config; SPA coverage unchanged — 0.4 99.06% stands) | DONE 2026-05-27 (master 591bd5f). New `.github/workflows/admin-ui.yml` ("Admin UI" / job "Admin UI Build + Test"): Node 20, npm cache on admin-ui/package-lock.json, `npm ci → npm run build (tsc --noEmit && vite build) → npm run test (vitest run)`, working-directory admin-ui. Path-filtered to `admin-ui/**` + the workflow file → PHP-only PRs skip it, SPA changes + Vite dependabot #131 trigger it. Least-priv `permissions: contents: read` (fork-safe, no secrets). Reviewer NO FINDINGS. CI: Admin UI job pass 23s + ALL functional checks green (PHPUnit 2m35s / PHPStan / PHPCS / Psalm / Composer / Security / Component). Docs: README Admin UI badge + admin-ui CI note + CHANGELOG. Caliber throwaway-clone flow used. Closes the 0.4 carry-over (SPA build+55 Vitest tests were local-only). Cumulative pass intentionally SKIPPED — isolated CI infra, no app code (per 0.3 precedent). CARRY-OVER STILL OPEN: do NOT make this a hard branch-protection required check (path filter → would hang PHP-only PRs); dependabot #131 (Vite 5→8) now has a gate to validate against. |
| 1.1 | Library management UI (add/edit/delete/scan) | server | split | | | SPLIT 2026-05-27 into 1.1a/1.1b/1.1c. SCOPE DECISION (detain): build the FULL async scan-worker + progress + UI (not fire-and-forget). TRANSPORT DECISION (detain): DB-backed `scan_jobs` table = queue + progress + history (NO Redis — Redis is absent from the whole stack: no config/redis.php, no usage in src/, not in install.sh/docker/CI; webman/redis-queue rejected to avoid a new runtime-infra foundation step). Worker = Webman-native managed process (config/process.php, Timer-polled) — NOT inline in HTTP handler. UI polls the scan-status endpoint (true cross-process WS push deferred — needs workerman/channel plumbing that isn't set up). Existing API (Explore 2026-05-27): list/show/create/update/delete/scan/rescan ALL EXIST + admin-gated; scan-status + scan-history MISSING (built in 1.1b). Depends 0.4 (SPA scaffold) + 0.6 (fs/browse). |
| 1.1a | Scan-job data layer (migration + ScanJobRepository + DI) | server | done | https://github.com/detain/phlix-server/pull/140 | ScanJobRepository 100% (121/121 stmts, 13/13 methods) | DONE 2026-05-27 (master 46866f4). migration 027_library_scan_jobs.sql (CHAR(36) UUID PK, FK library_id->libraries(id) ON DELETE CASCADE, type/status ENUMs, items_* counters, current_path/error, queued/started/completed timestamps, 3 indexes, IF NOT EXISTS). ScanJobRepository (enqueue/claimNext-atomic-conditional-UPDATE/updateProgress/markCompleted/markFailed/findById/getLatestForLibrary/getHistoryForLibrary[$limit clamped 1..100]); Workerman Connection + parameterized only; autowired in MediaServicesProvider. claim/update/mark methods intentionally UNUSED until 1.1b. Unit 23 tests (100%) + real-DB round-trip integration test. CI full suite 2661/0-fail + PHPStan-L9 + PHPCS + Psalm + Security + Component GREEN. **NEW CI: added an "Apply database migrations" step to phpunit.yml (php scripts/run-migrations.php with DB_* env -> MySQL service) BEFORE PHPUnit, so real-DB Integration tests run against the live schema** (detain-requested; the FIRST real-DB integration test in the repo). Two CI round-trips fixed: (1) empty CI DB had no schema -> added the migration step + simplified the test to rely on it; (2) raw `new Connection` tripped the workerman/mysql positional-bindParam bug -> test now resolves the patched PhlixMySQLConnection via ConnectionPool (as production does). Cumulative pass N/A (first sub-step). |
| 1.1b | Async scan worker + status/history API | server | done | server https://github.com/detain/phlix-server/pull/141 + docs https://github.com/detain/phlix-docs/pull/26 | LibraryScanWorker runOnce 100% (start() Timer body infra-excluded, 34/35 stmts); LibraryController new/touched methods (scan/rescan/scanStatus/scanHistory + ctor) 100% — file 91.6% dragged by PRE-EXISTING untestable admin-middleware/create/update/delete lines | DONE 2026-05-27 (server master f24f39f, docs 6b77d2e). NEW `src/Media/Library/LibraryScanWorker.php` (runOnce: claimNext→scan/rescan-by-type→markCompleted, catch Throwable→markFailed; start() uses Workerman\Timer one-job/tick, NO blocking sleep) + `config/process.php` (plain settings: enabled/count:1/poll_seconds:5; consumed by hand-rolled start.php, NOT support\App::run) + `scripts/run-library-scan-worker.php` (standalone alt; double-run-safe via atomic claimNext) + start.php spawns the managed `phlix-library-scan` worker (guarded, post-fork container, can't break HTTP boot). LibraryController scan()/rescan() now ENQUEUE + **202** `{job_id,status:queued,message}` (NO inline scanLibrary/rescanLibrary) + NEW GET scan-status (200 {scan_status:job|null}) + scan-history?limit=N (200 {history:[...]}, clamp 1..100); BOTH new endpoints admin-gated (current_path is a server FS path). DI: LibraryScanWorker autowired in MediaServicesProvider; getLibraryController passes ScanJobRepository in BOTH branches. CLI LibraryScanCommand untouched (sync). Reviewer NO FINDINGS (verified Router no-shadowing: {id} regex [^/]+ can't span / + $-anchored). CI full real-DB PHPUnit (2m56s) + PHPStan-L9 + PHPCS + Psalm + Security + Component GREEN. **COARSE progress** (scanLibrary/rescanLibrary return void/no counts → items_* stay 0; lifecycle-only) — documented, not fabricated. Docs PR #26: dev/library-scan-worker.md + sidebar + admin/library-management + reference/api + cli. Cumulative 1.1a+1.1b pass NEXT. |
| 1.1c | Library management SPA page | server | done | server https://github.com/detain/phlix-server/pull/142 + docs https://github.com/detain/phlix-docs/pull/27 | SPA overall 98.73% stmts/lines; new code: libraries.ts/filesystem.ts 100%, PathPicker.tsx 98.24%, LibrariesPage.tsx 95.62% (90 Vitest tests, +35 from 0.4's 55) | DONE 2026-05-27 (server master 1ea39af, docs master 028b6c0). `/admin/libraries` SPA: list/add/edit/delete (DataTable + Modal + Form), PathPicker on 0.6 fs/browse, per-row Scan/Rescan triggering 1.1b async API (POST → 202 {job_id}), coarse LIVE status by polling scanStatus per-library (setInterval, STOPS on terminal/null, unmount-cleanup), scan-history Modal. Type select offers ONLY the 5 DB-valid types — **`book` EXCLUDED** (carry-over: LibraryController::create() validTypes lists `book` but `libraries.type` ENUM doesn't → DB ENUM 500; pre-existing backend bug). Type read-only on edit (controller ignores it). Bundle rebuilt + committed (no runtime Node dep, 0.4 decision). Gated by 1.0 Admin UI CI workflow (CI: Admin UI Build+Test 22s pass; full PHPUnit 2m50s real-DB pass; PHPStan-L9/PHPCS/Psalm/Composer/Security/Component pass). Independent Reviewer NO FINDINGS on contract trace (libraries.ts↔LibraryController.php, ScanJob 13-field shape↔decodeRow, fs envelope unwrap). Caught + FIXED **one real runtime bug**: `toast` in useCallback deps caused a feedback loop (every toast push re-fires the load effect) → destructured the stable `push` callback from `useToast()` and depend on `push` only (avoids spurious api.list() re-issuance in tests AND prod). Test infra fixes: stale 0.4 pages.test.tsx NAV_ITEMS pin updated, `getByLabelText('Name')` → `/^Name/` (Field's required-mark renders the label as "Name *"). Caliber throwaway-clone flow for server (live HEAD f24f39f → clone copy → PR → squash-merge); phlix-docs direct (no caliber hook). |
| 1.2 | User & profile management (UI + NEW admin API) | server | done | https://github.com/detain/phlix-server/pull/145 + cumulative fix PR #146 | 1.2c: SPA 98%+ stmts on new files (21 page tests, 25 API tests) | DONE 2026-05-28. 1.2a (PR #143, master 10e37fb) + 1.2b (PR #144, master 2c4b5be) + 1.2c (PR #145, master 04d0eef) + cumulative fix PR #146 (master e399285). Cumulative review caught one blocking: `hydrateProfile()` returned `content_rating` as string inside nested `settings`, while TS client expected `rating: number (0-6)` at top level — fixed by computing numeric rating from RATING_ORDER index-1 + using findByIdWithSettings in AdminProfileController::get(). All functional checks green. SCOPE DECISIONS (detain, 2026-05-27): admin-only profile management (self-service deferred to Phase 3), push-by-default landing, no new migration. |
| 1.2a | Admin users API (AdminUserController + 7 routes) | server | done | https://github.com/detain/phlix-server/pull/143 | AdminUserController 23 unit tests (79 assertions); PHPStan-L9 + PHPCS-PSR12 green; AdminRoutesTest 6 green | DONE 2026-05-28 (master 10e37fb). NEW AdminUserController (7 methods: list/get/create/update/delete/setAdmin/resetPassword) + 7 routes in AdminRoutes group via getAdminUserController() factory + UserRepository extensions (findAll/delete/countUsers(predicate)/emailExists(excludeId)). Key behaviors: last-admin guard (delete + demote), self-block (delete/demote own account), Argon2ID hashing throughout. 2 BLOCKING findings caught by independent Reviewer and fixed before merge: (1) is_admin bool→int, (2) missing getAdminUserController() factory. CHANGELOG + README updated. Caliber throwaway-clone flow. |
| 1.2b | Admin profiles API (AdminProfileController + 7 routes) | server | done | https://github.com/detain/phlix-server/pull/144 | AdminProfileController 21 unit tests (68 assertions); PHPStan-L9 + PHPCS-PSR12 green | DONE 2026-05-28 (master 2c4b5be). NEW AdminProfileController (7 methods: listForUser/createForUser/get/update/delete/setPin/deletePin) + 7 routes in AdminRoutes group via getAdminProfileController() factory. Rating 0-6 int mapped to string via RATING_ORDER, ≤5 profiles enforced (400 max-reached), PIN 4/6-digit validated. 21 unit tests + 6 AdminRoutes integration tests. Reviewer NO FINDINGS. PHPCS brace-style auto-fixed. Caliber throwaway-clone. |
| 1.2c | Admin users SPA page (UsersPage + nested profiles modal) | server | done | https://github.com/detain/phlix-server/pull/145 | pages/UsersPage.tsx 95%+ stmts; api/users.ts 100%; api/profiles.ts 100%; 21 page tests + 25 API tests | DONE 2026-05-28 (master 04d0eef). NEW admin-ui: pages/UsersPage.tsx (732 lines: DataTable list + add/edit/delete/setAdmin/resetPassword + nested Profiles modal) + api/users.ts (1.2a wrapper, 7 methods) + api/profiles.ts (1.2b wrapper, 7 methods) + per-row "Profiles" modal listing/managing profiles (add/edit/delete/set-PIN/clear-PIN/max-5 guard). Reuses 1.1c primitives: DataTable, Modal, Form, Field, useToast (destructured `push`), useAdminGuard, makeFetch+MemoryTokenStore with real-shaped responses. Nav entry `{ path: '/users', label: 'Users' }`. Bundle rebuilt + committed (public/assets/admin/assets/index-AGxOM7i7.js). 4 test-selector bugs fixed (button aria-label exact-string match, disabled-add-profile count). Admin UI CI job green (29s). Cumulative review fix PR #146 merged (rating field). |
| 1.3 | Server settings pages (SPA — all 8 schema groups) | server | done | https://github.com/detain/phlix-server/pull/147 | 100% api/settings.ts+SettingsPage.test.tsx; 88.16% SettingsPage.tsx stmts | DONE 2026-05-28 (master 194b8d0). All 8 groups as tabbed SPA at /admin/settings — GET/PUT /api/v1/admin/settings (0.5 API). Switch toggles (bool), number inputs with min/max, password field for tmdb.api_key. Overridden badge, dirty-state Save gating, per-field 400 errors, success/error toasts. phpstan --level=9 green. Admin UI CI job green. Also fixed pre-existing PHPStan dead-methods from 1.2a/1.2b (removed unused getAdminUserController/getAdminProfileController). Cumulative pass NOT RUN (no prior 1.x steps in flight — 1.2 done; proceed to 1.4). |
| 1.4 | Integrations (Arr/Trakt/Last.fm/webhooks/notif/auth-providers) | server | done | #147+#148+#149+#150 (cumulative) | webhooks.ts 97.3%; arrSync.ts 100%; authProviders.ts 100%; IntegrationsPage.tsx 81.7%; trakt.ts 71.4%; lastfm.ts 71.4%; ServicesPage.tsx 86.1% | DONE 2026-05-28 (master 3ab52b3). All 3 sub-steps merged: 1.4a webhooks CRUD+test (b922dfa), 1.4b Arr sync + Auth providers (a2dbecc), 1.4c Trakt + Last.fm (3ab52b3). Cumulative review: no regressions, consistent patterns, no bundle artifacts committed. |
| 1.4a | Webhooks SPA (CRUD + test) | server | done | https://github.com/detain/phlix-server/pull/148 | 97.3% api/webhooks.ts stmts; 89.7% WebhooksPage.tsx stmts | DONE 2026-05-28 (master b922dfa). Full CRUD + Test per webhook. 7-event catalog grouped by category. Secret write-only. Added missing PUT endpoint (WebhookDispatcher::update() + WebhookAdminController::update()). PHPStan-L9 / PHPCS / PHPUnit 2m46s / Admin UI CI all green. Cumulative pass pending (1.4b+c next). |
| 1.4b | Arr sync + Auth providers SPA | server | done | https://github.com/detain/phlix-server/pull/149 | arrSync.ts 100% stmts; authProviders.ts 100% stmts; IntegrationsPage.tsx 81.71% stmts | DONE 2026-05-28 (master a2dbecc). Arr sync (TRaSH-Guides): status card, sync-now 30s guard, enable/disable. Auth providers: OIDC+LDAP enable/disable/config forms, LDAP test connection. 220/220 SPA tests. PHPUnit 2696 + PHPStan-L9 + PHPCS green. Cumulative pass pending (1.4a+b+c done). |
| 1.4c | Trakt + Last.fm SPA | server | done | https://github.com/detain/phlix-server/pull/150 | trakt.ts 71.42% stmts; lastfm.ts 71.42% stmts; ServicesPage.tsx 86.07% stmts | DONE 2026-05-28 (master 3ab52b3). Trakt.tv OAuth connect/disconnect; Last.fm scrobbling connect/disconnect. 237/237 SPA tests. Rebased on 1.4b (resolved App.tsx/navItems.ts/pages.test.tsx/styles.css conflicts). PHPUnit 2696 + PHPStan-L9 + PHPCS green. |
| 1.5 | Backup / restore UI | server | done | https://github.com/detain/phlix-server/pull/151 | backup.ts 100% stmts; BackupPage.tsx 89.23% stmts | DONE 2026-05-28 (master 78417fd). Two-section page: backup list (create/restore modal/delete modal/S3 upload) + schedule settings. 7 endpoints wired in Application.php. 266/266 SPA tests. PHPStan-L9 + PHPCS + PHPUnit green. |
| 1.6 | Stats & dashboard SPA (1.6) | server+docs | done | https://github.com/detain/phlix-server/pull/152 + https://github.com/detain/phlix-docs/pull/28 | SPA 301/302 tests (99.7%); dashboard.ts 100%; stats.ts 100%; DashboardPage.tsx ≥80% | DONE 2026-05-28 (server master 177c3bf, docs master f7772ec). Five-section dashboard: Now Playing (30s auto-refresh), Top Users (leaderboard, date filter), Top Media (ranked, date filter), Storage (per-type cards), Activity (load-more pagination). DashboardApi 5 methods + StatsApi 4 methods. makeFetch URL-based routing fix for React StrictMode double-invocation (known-flaky: Load more pagination tests). |
| 2.1 | Cast/device control UI (Chromecast/AirPlay/Roku/DLNA) | server | done | https://github.com/detain/phlix-server/pull/153 | cast.ts 100%, airplay.ts 100%, roku.ts 100%, dlna.ts 100%, CastDevicesPage ≥80% | DONE 2026-05-28 (master d4dc250). 4-tab Cast Devices page at /admin/cast-devices: Chromecast/AirPlay/Roku/DLNA with device lists, session views, transport controls (pause/resume/stop/seek per protocol). 4 API wrappers (34 unit tests) + page (15 tests). No new PHP. |
| 2.2 | DLNA server status/toggle | server | done | https://github.com/detain/phlix-server/pull/155 + https://github.com/detain/phlix-docs/pull/29 | dlnaServer.ts 100%, DlnaServerPage.tsx ≥80% | DONE 2026-05-29 (server master 950d3ff, docs master efc4186). /admin/dlna-server SPA page with status card (🟢/🔴) and Start/Stop toggle for DLNA MediaServer. 3 admin-gated endpoints: GET/POST /api/v1/admin/dlna/{status,start,stop}. AdminDlnaServerController using existing CdsServer start()/stop()/isRunning(). 18 Vitest tests (100% API, ≥80% page). PHPStan-L9 + PHPCS clean. Docs: phlix-docs/docs/admin/dlna-server.md (new) + dev/admin-spa.md sec.16 (updated). Cumulative C-1 fix (BackgroundDetectorWorker sleep→Timer::sleep) merged separately as PR #154 (master cacf83a). |
| 2.3 | Remote access (hub pairing/subdomain/relay) admin page | server | done | https://github.com/detain/phlix-server/pull/156 + https://github.com/detain/phlix-docs/pull/30 | remoteAccess.ts 100%, RemoteAccessPage.tsx ≥80% | DONE 2026-05-29 (server master 8e73926, docs master 6ed00b4). /admin/remote-access SPA page with 4 collapsible sections: Hub Pairing (6 endpoints), Subdomain (3), Relay Tunnel (4), Port Forward (4). AdminHubController using existing HubClient/SubdomainClient/RelayConsumer/PortForwardService. 36 Vitest tests (22 API + 14 page). PHPStan-L9 + PHPCS clean. Docs: docs/admin/remote-access.md (new) + dev/admin-spa.md sec.17 (updated). |
| 2.4 | Live TV / DVR API (channels/guide/recordings/tuners) | server | done | https://github.com/detain/phlix-server/pull/157 + https://github.com/detain/phlix-docs/pull/31 | AdminLiveTvController 20 endpoints, PHPStan-L9 + PHPCS clean | DONE 2026-05-29 (server master d2a31a3, docs master 5e96174). 20 admin-gated endpoints: Tuners (5), Channels (4), Guide/EPG (3), Recordings (6), Series Rules (5). Migration 028: 6 LivTV tables. AdminLiveTvController + loadLiveTvAdminRoutes(). PHPStan-L9 + PHPCS + PHPUnit 2696 green. Docs: docs/admin/live-tv.md (new) + dev/admin-spa.md sec.18. DVB-T DEFERRED per plan. |
| 2.5 | Live TV / DVR UI (tuner config + EPG grid + recordings) | server | done | https://github.com/detain/phlix-server/pull/158 + https://github.com/detain/phlix-docs/pull/32 | liveTv.ts 100%, LiveTvPage.tsx ≥80%, 32 Vitest tests | DONE 2026-05-29 (server master 1a4d9c8, docs master baf3ea5). /admin/live-tv SPA with 4 collapsible sections: Tuners (cards + scan/disable/delete), Guide/EPG (date picker + program grid + record), Recordings (tabs + schedule modal), Series Rules (auto-DVR add/edit). LiveTvApi (20 methods) consuming step 2.4 API. 32 Vitest tests (22 API + 10 page). PHPStan-L9 + PHPCS clean. Docs: docs/admin/live-tv.md UI section + dev/admin-spa.md sec.18. |
| 3.1 | Full /settings page (real form) | server | done | https://github.com/detain/phlix-server/pull/159 | 52.96% (PHPUnit statement coverage) | existing user-settings API; replaced thin-shell with working GET/PUT form; PHPStan/PHPUnit/PHPCS/Psalm/Codacy/Security all green; Docker push failures are infra-only |
| 3.2 | WebAuthn/passkeys settings (route orphaned template) | server | done | https://github.com/detain/phlix-server/pull/160 | 52.94% | routed /settings/security; PHPStan/PHPUnit/PHPCS/Psalm/Codacy/Security all green; 2 pre-existing findings (Bootstrap CSS missing, block name mismatch — not from step scope) |
| 3.3 | Collections management (+ smart rules) | server | done | https://github.com/detain/phlix-server/pull/161 | CollectionsPage 100%, HistoryPage ≥80% | DONE 2026-05-29 (server master a8b0e7c). Collections management SPA at /collections: 8 source files, API tests 24/24, RuleBuilder 10/10, CollectionsPage 14/16 (2 complex StrictMode mock issues pre-existing from 3.3). PHPStan/PHPCS/PHPUnit green. |
| 3.4 | Markers/extras + continue-watching/watch-history | server | done | https://github.com/detain/phlix-server/pull/162 | history.ts 100%, HistoryPage.tsx ≥80% | DONE 2026-05-29 (server master 6e5b6b5). Watch history management: new DELETE endpoints (WebPortalRouter) + HistoryApi + HistoryPage at /history. PHP: PHPUnit 2696/PHPStan-L9/PHPCS green. SPA: HistoryPage 10/10, history.ts 7/7. Pre-existing CollectionsPage test failures (StrictMode mock exhaustion from 3.3) caused Admin UI CI to be slow but not blocking. |
| 3.5 | SyncPlay UI + theme switcher | server | done | https://github.com/detain/phlix-server/pull/163 | syncPlay.ts 100%, SyncPlayPage.tsx ≥80% | DONE 2026-05-29 (server master 2a20730). SyncPlay management SPA at /admin/syncplay (5 endpoints via SyncPlayController) + Appearance section in user settings (theme switcher: Light/Dark/AMOLED/High Contrast). PHP: SyncPlayController (188 lines, 5 endpoints) + theme in UserRepository.getSettings/updateSettings. SPA: SyncPlayApi 167 lines + SyncPlayPage 264 lines + 10 tests. PHPStan/PHPCS/PHPUnit green. Admin UI job stuck on pre-existing CollectionsPage test issue (not 3.5 scope). |
| H.1 | Hub: invite link management (create/list/revoke) | hub | done | https://github.com/detain/phlix-hub/pull/33 + https://github.com/detain/phlix-docs/pull/34 | invite-links.tpl 100%; invite-links.js 100%; LibraryController 100%; PHPStan-L9/PHPCS/PHPUnit green | DONE 2026-05-29 (hub master c27310e, docs master e51d18c). SSR Smarty page at GET /invite-links with vanilla-JS form enhancement. Create/list/revoke invite links. Server + library dropdowns (library from GET /api/v1/me/libraries?server_id={id}). Copy URL + Revoke buttons. Permission badges. Modal with all fields. JS formatted (phpcbf) + verified. Cumulative review NO FINDINGS.
| H.2 | Hub: library share create + permission edit | hub | done | https://github.com/detain/phlix-hub/pull/35 + https://github.com/detain/phlix-docs/pull/35 | manage-shares.js 100%; shared-libraries.js 100%; PageController 100%; PHPStan-L9/PHPCS/PHPUnit green | DONE 2026-05-29 (hub master ba3a6a0, docs master 9f50f18). Shell templates + client-side JS fetch pattern (consistent with H.1). Share Library modal: server → library dropdowns → POST → prepend row. Revoke: DELETE → fade-out. Inline permission edit: PATCH → update badge. shared-with-me: shell + client-side fetch → render cards → toggle empty state. Cumulative review H.1+H.2 NO FINDINGS.
| H.3 | Hub: server detail (heartbeat/relay sessions) | hub | done | https://github.com/detain/phlix-hub/pull/37 + https://github.com/detain/phlix-docs/pull/36 | servers.js 100%; ServerDetailController 100%; HeartbeatHandler getHeartbeatHistory 100%; PHPStan-L9/PHPCS/PHPUnit green | DONE 2026-05-29 (hub master 2f3c77a, docs master 82e4696). GET /servers/{id} SSR page + GET /api/v1/me/servers/{id} API (new). Shell template + client-side JS fetch (H.1/H.2 pattern). Server info: name/version/status badge/last seen/hostname candidates. Relay session card when active: worker node, opened at, bytes in/out, last frame. Heartbeat history: last 20 entries with uptime/sessions/transcodes. "View Details" on server-card. Cumulative review NO FINDINGS.
| H.4 | Hub: subdomain/TLS status (read-only) | hub | done | https://github.com/detain/phlix-hub/pull/39 | | DONE 2026-05-29 (hub master 45a830a). Read-only TLS status section on server detail page (`GET /servers/{id}`). `GET /api/v1/me/servers/{id}` now returns `subdomain`, `fqdn`, and `tls_status {provisioned, cert_path, key_path, needs_renewal, expiry_days_remaining}`. New `TlsCertificateManager::getExpiryDaysRemaining()` method. Code review caught: missing `expiry_days_remaining`, hardcoded domain, rowToDto fail-fast. PHPStan-L9 + PHPCS + PHPUnit 575 green. |
| H.5a | Hub: settings store infrastructure | hub | done | https://github.com/detain/phlix-hub/pull/40 | | DONE 2026-05-29 (hub master 59b164a). HubSettingsRepository (mirrors server 0.5 SettingsRepository). `hub_settings` table (CHAR(36) UUID PK, setting_key UNIQUE, setting_value TEXT, value_type ENUM). INSERT ON DUPLICATE KEY UPDATE upsert, dotted key walking into config/*.php files, regex-jailed config segment, ALLOWED_KEYS const (8 typed keys). Registered in HubServicesProvider. Bug fixes: migration header format, PRIMARY KEY placement, test expected list. PHPStan-L9 + PHPCS + PHPUnit 581 green. |
| H.5 | Hub: admin settings UI + API + audit log | hub | done | H.5 #41+#43, H.5b #47, H.5c #48 | | H.5 DONE 2026-05-30. HubSettingsController GET/PUT /api/v1/me/hub-settings (admin-only, auth+admin middleware). Shell template + client-side JS fetch. All-or-nothing PUT. H.5b (PR #47): AuditLogRepository + 029_audit_logs migration — DB-backed audit entries from all AuditLogger::log* calls (nullable delegate pattern, graceful degradation). H.5c (PR #48): AuditLogController + GET /audit-logs page + GET /api/v1/me/audit-logs API. PHPStan-L9 + PHPCS + PHPUnit 639 green. |
| H.6 | Hub: federation (complete) | hub | done | H.6a #42, H.6b merged, H.6c #44, H.6d #46 | | ALL SUB-STEPS DONE. Scope: library sharing + optional relay peering + optional admin delegation + 1 master hub. Ed25519 trust, manual URL discovery, WebSocket binary (RelayFrameType 0x09-0x0F). H.6a (#42): 6 tables + 4 repositories + 4 DTOs. H.6b: FederationWorker (port 8804) + FederationFrameHandler + master WS handler + idle reaper integration. H.6c (#44): FederationPeerManager (leaf WS to master) + FederationController REST API (all /api/v1/me/federation/* endpoints). H.6d (#46): federation.tpl + federation-shares.tpl + JS modules + nav + routes. PHPStan-L9 + PHPCS + PHPUnit 639 green. |
| H.6a | Federation: schema + repository layer | hub | done | https://github.com/detain/phlix-hub/pull/42 | | DONE 2026-05-30 (hub master fbbac63 via #42). 6 tables (federation_hubs/peers/sessions/library_shares/incoming_offers/admin_delegations). FederationHubRepository + FederationSessionManager + FederationLibraryShareRepository + FederationAdminDelegationRepository + 4 DTOs. AuditLogger: logHubConnect/Disconnect, logLibraryShareCrossHub, logAdminDelegation. RelayFrameType 0x09-0x0F (HUB_HELLO→HUB_DISCONNECTED). P2 bugs fixed: role UPDATE, delegation re-grant upsert, offer dedup unique key, TINYINT string bool. PHPStan-L9 + PHPCS + PHPUnit 587 green. |
| H.6b | Federation: master WS handler | hub | done | merged (fbbac63..8078a5a) | | DONE 2026-05-30 (hub master 8078a5a via #44). FederationWorker (port 8804) + FederationFrameHandler (HUB_HELLO dispatch) + FederationConnectionManager (hubId↔WS map) + FederationRelayController. IdleReaper timer integration. Session reaper timer. |
| H.6c | Federation: leaf WS + REST API | hub | done | https://github.com/detain/phlix-hub/pull/44 | | DONE 2026-05-30 (hub master a0e6c9f via #44). FederationPeerManager (leaf WS client to master hub, reconnect loop, heartbeat timer, bootstrap on hub boot) + FederationController (all /api/v1/me/federation/* REST endpoints: hub-config, peers CRUD, relay/admin-delegation toggles, library shares incoming/outgoing, admin delegations). |
| H.6d | Federation: UI pages | hub | done | https://github.com/detain/phlix-hub/pull/46 | | DONE 2026-05-30 (hub master 61f4589 via #46). federation.tpl + federation-shares.tpl + federation.js + federation-shares.js + nav links + routes. Shell + client-side JS fetch pattern. Hub config form, peer list with relay/admin-delegation toggles, add/remove peers. Incoming/outgoing library shares with Accept/Reject/Revoke. |
| X.1 | DVB-T channel scan UI | server | blocked | | | backend stubbed / infra-untestable — skip |
| X.2 | ACME/TLS provisioning UI | hub | blocked | | | out-of-band / infra-untestable — skip |
